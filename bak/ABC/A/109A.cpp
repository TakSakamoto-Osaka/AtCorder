#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for ( int i = 0; i < (n); i++ )

int main() {
    int A, B;

    cin >> A >> B;

    if ( A % 2 == 0 || B % 2 == 0 ) {
        cout << "No" << endl;
    } else {
        cout << "Yes" << endl;
    }
}
